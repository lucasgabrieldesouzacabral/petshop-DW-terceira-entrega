<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listar Serviços</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 1200px;
            margin: 20px auto;
            padding: 20px;
        }
        h1 {
            color: #2c3e50;
        }
        .nav-links {
            margin-bottom: 20px;
        }
        .nav-links a {
            margin-right: 15px;
            text-decoration: none;
            color: #3498db;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .btn-new {
            background-color: #27ae60;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 4px;
            display: inline-block;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: white;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }
        th {
            background-color: #3498db;
            color: white;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        .btn {
            padding: 5px 10px;
            text-decoration: none;
            border-radius: 4px;
            font-size: 14px;
            margin-right: 5px;
        }
        .btn-view {
            background-color: #27ae60;
            color: white;
        }
        .btn-edit {
            background-color: #3498db;
            color: white;
        }
        .btn-delete {
            background-color: #e74c3c;
            color: white;
            border: none;
            cursor: pointer;
        }
        .status-badge {
            padding: 3px 8px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: bold;
        }
        .pendente {
            background-color: #f39c12;
            color: white;
        }
        .realizado {
            background-color: #27ae60;
            color: white;
        }
        .cancelado {
            background-color: #e74c3c;
            color: white;
        }
    </style>
</head>
<body>
    <h1>Listar Serviços</h1>
    <div class="nav-links">
        <a href="/">🏠 Início</a>
    </div>
    
    <a href="/servicos/novo" class="btn-new">Cadastrar Novo Serviço</a>
    
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Animal</th>
                <th>Tutor</th>
                <th>Tipo de Serviço</th>
                <th>Funcionário</th>
                <th>Produto</th>
                <th>Data</th>
                <th>Valor</th>
                <th>Status</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            {{#each servicos}}
            <tr>
                <td>{{id}}</td>
                <td>{{animalNome}}</td>
                <td>{{tutorNome}}</td>
                <td>{{tipoServico}}</td>
                <td>{{funcionarioNome}}</td>
                <td>{{produtoNome}}</td>
                <td>{{data}}</td>
                <td>R$ {{valor}}</td>
                <td><span class="status-badge {{status}}">{{status}}</span></td>
                <td>
                    <a href="/servicos/{{id}}" class="btn btn-view">Ver</a>
                    <a href="/servicos/{{id}}/editar" class="btn btn-edit">Editar</a>
                    <form action="/servicos/{{id}}/excluir" method="post" style="display: inline;">
                        <button type="submit" class="btn btn-delete" onclick="return confirm('Tem certeza que deseja excluir este serviço?')">Excluir</button>
                    </form>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>

